import styled from "styled-components";
import { color, font } from "../../constants/styleVariables";

export const MediumClapPageContainer = styled.main`
  color: ${color.font};
  font-family: ${font.AvenirNunito};
  font-size: 16px;
  font-weight: 500;
`;
